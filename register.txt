html code
<!DOCTYPE html>
<html>
<head>
    <title>Registration Form</title>
    <style>
        body {
    font-family: Arial;
    background-color: #e6f2ff;
}

.container {
    width: 300px;
    margin: 50px auto;
    padding: 20px;
    background: white;
    border: 1px solid #ccc;
}

input {
    width: 100%;
    padding: 8px;
    margin: 5px 0;
}

.error {
    color: red;
    font-size: 12px;
}

button {
    width: 100%;
    padding: 10px;
    background: blue;
    color: white;
    border: none;
}
    </style>
</head>
<body>

<div class="container">
    <h2>Registration Form</h2>

    <form onsubmit="return validateForm()">
        
        <label>Name</label>
        <input type="text" id="name">
        <p id="nameError" class="error"></p>

        <label>Password</label>
        <input type="password" id="password">
        <p id="passError" class="error"></p>

        <label>Email</label>
        <input type="text" id="email">
        <p id="emailError" class="error"></p>

        <label>Phone</label>
        <input type="text" id="phone">
        <p id="phoneError" class="error"></p>

        <button type="submit">Register</button>
    </form>
</div>

<script src="script.js"></script>
</body>
</html>

js code

function validateForm() {

    let name = document.getElementById("name").value;
    let password = document.getElementById("password").value;
    let email = document.getElementById("email").value;
    let phone = document.getElementById("phone").value;

    // Clear errors
    document.getElementById("nameError").innerHTML = "";
    document.getElementById("passError").innerHTML = "";
    document.getElementById("emailError").innerHTML = "";
    document.getElementById("phoneError").innerHTML = "";

    let valid = true;

    // Name validation
    let namePattern = /^[A-Za-z]{6,}$/;
    if (!namePattern.test(name)) {
        document.getElementById("nameError").innerHTML = "Enter at least 6 alphabets";
        valid = false;
    }

    // Password validation
    if (password.length < 6) {
        document.getElementById("passError").innerHTML = "Minimum 6 characters required";
        valid = false;
    }

    // Email validation
    let emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailPattern.test(email)) {
        document.getElementById("emailError").innerHTML = "Invalid email";
        valid = false;
    }

    // Phone validation
    let phonePattern = /^[0-9]{10}$/;
    if (!phonePattern.test(phone)) {
        document.getElementById("phoneError").innerHTML = "Enter 10 digits";
        valid = false;
    }

    if (valid) {
        alert("Registration Successful!");
    }

    return valid;
}